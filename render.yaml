services:
  - type: web
    name: medical-report-extractor
    env: python
    buildCommand: |
      pip install -r requirements.txt
      cp nginx.conf /etc/nginx/conf.d/custom.conf
    startCommand: gunicorn app:app --timeout 300 --workers 1 --threads 1 --max-requests 1 --max-requests-jitter 1
    envVars:
      - key: OPENAI_API_KEY
        sync: false
    plan: free
    healthCheckPath: /
    autoDeploy: true 